Utiliza o npm install para instalar as dependências
npx expo start para iniciar a aplicação
A aplicação só vai rodar se estiver com o banco de dados do site motiva rodando, para isso utiliza o cd back-end para entrar na pasta e python rodar-back.py para rodar todos os arquivos
